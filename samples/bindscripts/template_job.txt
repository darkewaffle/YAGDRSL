-- 1 to 5
bind %1 input 
bind %2 input 
bind %3 input 
bind %4 input 
bind %5 input 

-- Shift 1 to Shift 5
bind ~%1 input 
bind ~%2 input 
bind ~%3 input 
bind ~%4 input 
bind ~%5 input 

-- QWERT & Shift QWERT
bind %q input 
bind ~%q input 

bind %w input 
bind ~%w input 

bind %e input 
bind ~%e input 

bind %r input 
bind ~%r input 

bind %t input 
bind ~%t input 

-- ASDFG & Shift ASDFG
bind %a input 
bind ~%a input 

bind %s input 
bind ~%s input 

bind %d input 
bind ~%d input 

bind %f input 
bind ~%f input 

bind %g input 
bind ~%g input 

-- XCVB & Shift XCVB
bind %x input 
bind ~%x input 

bind %c input 
bind ~%c input 

bind %v input 
bind ~%v input 

bind %b input 
bind ~%b input 

-- Tick/Tilde
bind %` input 
bind ~%` input 