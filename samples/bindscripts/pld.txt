-- 1 to 5
-- Weaponskills
bind %1 input /ws "Savage Blade" <t>
bind %2 input /ws "Chant du Cygne" <t>
bind %3 input /ws "Atonement" <t>
bind %4 input /ws "Requiescat" <t>
bind %5 input /ws "Circle Blade" <t>
--bind %5 input /ws "Aeolian Edge" <t>

-- Shift 1 to Shift 5
bind ~%1 input /ma "Reprisal" <me>
bind ~%2 input /ma "Enlight II" <me>
bind ~%3 input /ma "Phalanx" <me>
--bind ~%4 input /ma "Stutter Step" <t>
bind ~%5 input /ma "Crusade" <me>


-- QWERT & Shift QWERT
bind %q input /ja "Shield Bash" <t>
bind ~%q input /ja "Cover" <stpt>

bind %w input /ma "Cure IV" <me>
bind ~%w input /ma "Cure IV" <stpt>

bind %e input /ma "Cure" <me>
bind ~%e input /ma "Cure" <stpt>

bind %r input /ja "Sentinel" <me>
bind ~%r input /ja "Rampart" <me>

bind %t input /ja "Palisade" <me>
bind ~%t input /ja "Divine Emblem" <me>


-- ASDFG & Shift ASDFG
bind %a input /attack <t>
bind ~%a input /ra <t>

bind %s input /check <t>
bind ~%s input /targetbnpc

bind %d input /ma "Banishga" <stnpc>
--bind ~%d input /ja "Ternary Flourish" <me>

bind %f input /ma "Flash" <t>
bind ~%f input /ma "Flash" <stnpc>

bind %g input /ja "Majesty" <me>
--bind ~%g input /ja "Divine Waltz" <me>


-- XCVB & Shift XCVB
bind %x input /ja "Invincible" <me>
bind ~%x input /ja "Intervene" <t>

bind %c input /ja "Fealty" <me>
bind ~%c input /ja "Chivalry" <me>

bind %v input /ja "Holy Circle" <me>
bind ~%v input /ja "Sepulcher" <t>

bind %b input /map
--bind ~%b input 

-- Tick/Tilde
bind %` input /mount "Noble Chocobo"
bind ~%` input /dismount