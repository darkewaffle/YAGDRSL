# Step 3 - Overrides for All Occasions

The override is a special type of mod. It works just like a mod except it uses it's own gearset table and there's only one override - and it's always given priority over other mods no matter what.